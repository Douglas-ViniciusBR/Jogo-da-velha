#include <stdio.h>

char jogo[3][3];
char player1[20], player2[20];

void inicializarMatriz(){
    int i, j;
    for (i = 0; i <3; i++)
        for (j =0 ; j < 3; j++)
            jogo [i][j] + 'v';

}

int Valido (char letra){
    if (letra == 'x' || letra == '0')
         return 1;
    else
         return 0;

}

int coordenadaValida (int x, int y){
if (x >=0 && x < 3){
    if (y >=0 && y < 3)
        return 1;

 }
 return 0; 
}

int posicaoVazia ( int x, int y){
    if (jogo [x][y] != 'x' && jogo [x][y] != '0')
        return 1;
    else
        return 0;
}

int ganhouLinhas(){
    int i, j, igual = 1;
    for(i =0; i < 3; i++){
        for(j = 0; j < 2; j++){
            if (Valido(jogo[i][j]) && jogo [i][j] == jogo [i][j] + 1 ){
                igual ++;
            }
            if (igual == 3)
                 return 1;
            igual = 1;
        }
        return 0;
    }
}

int ganhouColunas(){
    int i, j, igual = 1;
    for(i = 0; i< 3; i++){
        for(j = 0; j < 2; j++){
            if(Valido(jogo[j][i]) && jogo [j][i] == jogo [j+1][i]){
              igual ++;
            }    
            if(igual == 3)
                 return 1;  
                 igual = 1;    
        }
        return 0;
    }
}
int ganhoudiagPrincipal(){
  int i, igual = 1;
  for(i = 0; i < 2; i ++){
      if(Valido(jogo[i][i]) && jogo [i][i] == jogo [i+1][i+1]){
          igual ++;
      }
      if(igual == 3)
          return 1;
      else
          return 0;    
  } 
}

int ganhoudiagSecundaria(){
  int i, igual = 1;
  for(i = 0; i < 2; i ++){
      if(Valido(jogo[i][3-i-1]) && jogo [i][3-i-1] == jogo [i+1][3-i-2]){
          igual ++;
      }
      if(igual == 3)
          return 1;
      else
          return 0;    
  } 
}

void imprimir(){
    int l, c;
    printf("\t0 1 2\n");
    for(l = 0; 1 < 3; l++){
      printf("%d", l);
      for(c = 0; c < 3; c++){
           if(Valido(jogo [l][c])){
              if(c < 2)
              printf (" %c |", jogo [l][c]);
              else
               printf (" %c ", jogo [l][c]);
           }   
          else
              if(c < 2)
                  printf("  |");
              else
                  printf("   ");
        }  
        printf("\n----------\n");
      
    }

}

int main()
{ 



}
